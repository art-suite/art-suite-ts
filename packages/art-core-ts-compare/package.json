{
  "name": "@art-suite/art-core-ts-compare",
  "version": "0.0.8",
  "description": "A TypeScript comparison utility library",
  "keywords": [],
  "repository": {
    "type": "git",
    "url": "https://github.com/art-suite/art-suite-ts"
  },
  "license": "MIT",
  "author": "Shane Delamore",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "npm run clean",
    "build": "npx sort-package-json;tsup src/index.ts --format esm,cjs --dts --sourcemap",
    "build:clean": "npm run clean && npm run build",
    "check:types": "tsc --noEmit",
    "check:types:fast": "tsc --noEmit --incremental",
    "clean": "rm -rf dist",
    "perf": "node performance-test.js",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@art-suite/art-core-ts-containers": "^0.9.6",
    "@art-suite/art-core-ts-types": "^0.3.8"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.5"
  }
}
